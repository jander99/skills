# Lessons Learned

> Auto-generated by the `retro` skill. Each entry follows the Sailboat+Forward schema.
> Reconstructed from context window (session_read unavailable).

<!-- retro:entries:2 -->

## 2026-03-14 17:53 | retro dual-location shell-flags docs
> Trigger: when changing `skills/retro/scripts/retro-lessons.sh` to add new location flags or multi-source retrieval/injection behavior
> Action: test every affected subcommand (`retrieve`, `inject`, `validate`, `count`) against global-only, local-only, both, and missing-file cases; keep `SKILL.md` and reference examples in sync with the implemented flags before calling the feature done
> Scope: skills/retro/**

**Wind 🌬️:**
- Reviewing the branch from a clean worktree and exercising the script in temp directories surfaced behavioral gaps quickly.
- The dual-location direction itself is sound; most issues are contract drift rather than a bad design.

**Anchor ⚓:**
- `--both` was documented more broadly than it was implemented, so `validate` and `count` did not match the CLI contract.
- `inject --both` used split budgeting with cumulative token estimation, and `retrieve --both` concatenated per-file results instead of merging them globally.

**Rocks 🪨:**
- A single sentinel at `~/.agents/.retro-pending` can collide across concurrent repos or sessions unless it is namespaced.
- Stale examples in `skills/retro/references/*.md` and comments can retrain agents toward the old single-location behavior.

**Next 🧭:**
- Add regression tests for `--global` / `--local` / `--both` across all subcommands, including missing-file cases, and sweep the retro docs/comments for stale examples.

---

## 2026-03-14 22:12 | retro verification silent-success
> Trigger: when final-verifying `skills/retro/scripts/retro-lessons.sh` behavior, especially dual-location flows where some success paths are intentionally quiet
> Action: verify quiet subcommands by exit status with an explicit success marker, and pair CLI output checks with a direct source grep for the expected guard expression
> Scope: skills/retro/**

**Wind 🌬️:**
- Running the full five-command verification matrix from the feature worktree gave fast confidence that retrieval, injection, missing-file validation, and the loop guard were all aligned.
- Checking both observable output and the script source caught the exact contract the user asked to confirm.

**Anchor ⚓:**
- `validate --both` succeeds silently, so stdout alone is not enough to prove the command passed during a final verification run.
- A fallback from `python` to `python3` was needed just to print an explicit success marker in this shell environment.

**Rocks 🪨:**
- Future refactors could preserve the CLI shape while regressing the internal cap logic unless the source-level guard is still checked.

**Next 🧭:**
- Keep the release-style verification recipe as: `validate --both`, injected bullet count, `retrieve --both --recent N`, missing-file validate, and `grep` for `count<max_bullets`.
